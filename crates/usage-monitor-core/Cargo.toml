[package]
name = "usage-monitor-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "UsageMonitor core engine — UnifiedMessage, TokenBreakdown, DataSource trait, scanner, aggregator"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
walkdir = "2"
chrono = "0.4"

[dev-dependencies]
tempfile = "3"
